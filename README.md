
```
├── README.md                       # this readme
├── .gitignore                      # logs/, *.log, midnight-level-db/, reports/, .DS_Store
├── .env.example
├── package.json
│
├── docs/
│   ├── architecture.md             # full flow: fund derived account → deposit() → MPC signs →
│   │                               #   postResponse() on signature-responses contract → watcher
│   │                               #   submits EVM tx → MPC confirms → Schnorr response → claim()
│   └── e2e-sepolia-runbook.md      # moved from boilerplate/contract-cli/docs/
│
├── scripts/                        # repo-level orchestration only (bash / plain node, no build step)
│   └── placeholder.sh 
│
└── packages/
    ├── lib/                        # @midnight-erc20-vault/lib
    │   ├── package.json            # shared runtime plumbing — the ONLY copy of these files;
    │   ├── tsconfig.json           #   deploy, integration-tests and testing all import from here
    │   └── src/
    │       └── index.ts
    │
    ├── signet-midnight/            # @midnight-erc20-vault/signet-midnight
    │   ├── README.md
    │   ├── package.json
    │   ├── tsconfig.json
    │   └── src/
    │       └── index.ts
    │
    ├── vault-contract/             # @midnight-erc20-vault/vault-contract
    │   ├── package.json            # scripts: compile, compile:zk, build (tsc), test, deploy
    │   ├── tsconfig.json
    │   ├── .gitignore              # src/managed/  (generated — never committed)
    │   ├── src/
    │   │   ├── erc20-vault.compact
    │   │   ├── schnorr-lib.compact
    │   │   ├── witnesses.ts        # handwritten witnesses live beside the contract they serve
    │   │   ├── index.ts            # curated export surface — this IS the "sdk" face of the package
    │   │   └── managed/            # generated by compactc; gitignored
    │   ├── tests/
    │   │   └── contract.test.ts    # simulator-level unit tests, no network
    │   └── deploy.ts               # thin entrypoint: calls @.../deploy with this package's managed/
    │                               #   path, then runs initialise() as a normal circuit call
    │
    ├── signature-responses-contract/   # @midnight-erc20-vault/signature-responses-contract
    │   ├── package.json
    │   ├── tsconfig.json
    │   ├── .gitignore                  # src/managed/
    │   ├── src/
    │   │   ├── signature-responses.compact
    │   │   ├── witnesses.ts            # localSecretKey (owner proof)
    │   │   ├── index.ts
    │   │   └── managed/
    │   ├── tests/
    │   │   └── contract.test.ts
    │   └── deploy.ts
    │
    └── deploy/                     # @midnight-erc20-vault/deploy
        ├── README.md               # generic Midnight deployer: point it at a managed/ dir, it builds
        ├── package.json            #   and submits the deploy tx (midday buildDeployTransaction
        ├── tsconfig.json           #   pattern). Generalisable because our contracts initialise via a
        └── src/                    #   post-deploy circuit call, not constructor args.
            ├── index.ts
            └── deploy.ts
```