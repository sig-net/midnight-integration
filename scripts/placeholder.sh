#!/usr/bin/env bash
# scripts/ holds repo-level orchestration only (bash / plain node, no build step).
# The local Midnight stack definition (standalone.yml, proof-server.yml,
# wait-for-stack.sh) and run-integration-local.sh arrive with the
# integration-tests step. This placeholder keeps the directory in git until then.
exit 0
