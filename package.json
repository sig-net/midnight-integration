{
  "name": "midnight-erc20-vault",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Demonstrates sig-net cross-chain interactions from Midnight: an ERC20 vault driven by MPC-signed EVM transactions. Run `npm run compile` once before `build`/`test` — the contract packages typecheck against the generated managed/ output.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "scripts/compile.sh",
    "compile:zk": "scripts/compile.sh --zk",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "deploy:vault": "npm run deploy -w @midnight-erc20-vault/vault-contract",
    "deploy:responses": "npm run deploy -w @midnight-erc20-vault/signature-responses-contract"
  }
}
