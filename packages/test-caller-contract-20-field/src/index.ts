// Curated export surface: the compiler output alone. The contract declares
// no witnesses and no constructor arguments; it exists only for the
// chunked-ledger simulator tests in tests/.

export * from "./managed/contract/index.js";
