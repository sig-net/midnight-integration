{
  "name": "@midnight-erc20-vault/signet-midnight",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Midnight-side sig-net integration — the seed of a future signet.js Midnight adapter.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
