// Public surface of @midnight-protocol/lib — the midnight-js proof provider
// shared by the contract packages' provider sets. The wallet (interface,
// implementations, seed/key/config plumbing) lives in
// @sig-net/midnight-wallet; generic deploy plumbing lives in
// @sig-net/midnight-contract-deploy.
export * from "./midnight-providers.ts";
