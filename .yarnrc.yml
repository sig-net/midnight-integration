# node-modules linker is required: the @midnight-ntwrk wasm/native packages and
# the compact compiler's COMPACT_PATH=../../node_modules imports need a real
# hoisted node_modules tree (PnP would break both), and the response server's
# cross-repo symlink fix points into this tree.
nodeLinker: node-modules

enableTelemetry: false

# All script bearing dependency packages ship prebuilt binaries.
# Set to true if building on an unsupported platform.
enableScripts: false
